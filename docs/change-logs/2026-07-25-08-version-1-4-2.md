# 版本 1.4.2

## 目的

为本地行动中心功能创建新的补丁版本，更新营销版本号和构建号，并生成可用于 AltStore 的发布包。

## 影响文件

- `project.yml`
- `FlashCount.xcodeproj/project.pbxproj`（由 XcodeGen 重新生成）
- `docs/change-logs/2026-07-25-08-version-1-4-2.md`
- `build/FlashCount-AltStore.ipa`（本地构建产物，不纳入 Git 提交）

## 行为变化

- `MARKETING_VERSION` 从 `1.4.1` 更新为 `1.4.2`。
- `CURRENT_PROJECT_VERSION` 从 `5` 更新为 `6`。
- 不改变业务逻辑、数据格式、网络行为或 SwiftData 模型。

## 验证

- `xcodegen generate`：通过。
- `git diff --check`：通过。
- `./scripts/package-altstore.sh`：通过，生成 `build/FlashCount-AltStore.ipa`。
- IPA 信息核验：版本 `1.4.2 (6)`、架构 `arm64`、仅包含 `Payload/FlashCount.app`，无 `.appex`、`_CodeSignature` 或 `embedded.mobileprovision`。
- IPA SHA-256：`b2907243e33e30c2b97c5726302ab15bd105c2cd9c50d9433b988e55fd17be2f`。

## 限制

- 本次按项目默认发布规则生成未签名 AltStore 包；未生成需要指定分发渠道和签名配置的正式 App Store/TestFlight IPA。
