# Recurring rules: end date and skip

## Changes

- Periodic rules can now have an optional end date.
- Due processing automatically pauses a rule when its next occurrence exceeds that end date.
- Rule management offers “skip this period”, advancing the schedule without creating a transaction.
